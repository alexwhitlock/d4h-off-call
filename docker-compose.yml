services:
  d4h-off-call:
    build: .
    image: d4h-off-call:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:5002:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
